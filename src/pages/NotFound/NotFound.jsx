import not from '../../assets/images/login/404.png'

const NotFound = () => {
    return (
        <div className="flex items-center justify-center">
            <img src={not} alt="" />
        </div>
    );
};

export default NotFound;